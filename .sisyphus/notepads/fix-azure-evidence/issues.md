Issues encountered:
- Live Azure lab not accessible; needing synthetic fixtures.
- Some historical evidence files required header annotations; content preserved.
- Plan file is read-only per governance, so certain F fixes cannot be applied directly to the plan.

Next steps: coordinate with orchestrator to enable Plan 9 changes or create a parallel changelog entry instead of editing the plan.
