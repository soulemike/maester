Task 3 - Lab hardening runner, identity, and TLS trust evidence
Date: 2026-09-18
Method: Static deployment-automation assertions (Azure resources were not deployed by this task)

PASS: Exactly one DC trust anchor required per DC
PASS: LDAP certificate has server-auth EKU and SAN input
PASS: NTDS reloads LDAP certificate
PASS: Windows trusts DC certificates in LocalMachine Root
PASS: Ubuntu installs PEM trust anchors
PASS: Windows runner joins root forest
PASS: Ubuntu runner enrolls with realmd and SSSD
PASS: No cross-forest trust; explicit credential required

Identity and credential contract:
- Windows ambient identity: a logged-in misoule02.local domain user on MSRunnerWin.
- Linux ambient identity: a fresh pwsh process launched as the SSSD-resolved `maesterjoin@misoule02.local` user; Azure Run Command's root identity is not accepted as proof.
- Runner local administration, root-domain enrollment through low-privilege `maesterjoin`, and forest administration use distinct generated credentials.
- Child authentication: implicit through the automatic intra-forest trust or an explicit CHILD credential.
- Separate-forest authentication: explicit MISOULE03 credential only. No forest trust exists, and no trust-aware separate-forest success row is expected.

TLS contract:
- Every DC certificate contains Server Authentication EKU plus the DC short name, DC FQDN, and domain name as SANs.
- The certificate is available to AD DS for LDAPS on 636 and StartTLS on 389.
- Deploy-Lab.ps1 aborts unless it receives one certificate from each of DC02, DC03, and DC04.
- New-RunnerVm.ps1 installs all certificates in Windows LocalMachine\Root and converts them to PEM before Ubuntu system-CA installation.
- Open ports are not treated as proof of certificate trust or TLS negotiation.

Live bind, chain, and StartTLS negotiation checks belong to the post-deployment
hard preflight. This evidence records the provisioning guarantees without
claiming live Azure validation that was not performed.
